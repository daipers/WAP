---
phase: 04-integrity-identity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - dev_package/src/delivery_service/accommodations.py
  - dev_package/src/delivery_service/session_manager.py
  - dev_package/src/lti_service/lti_service.py
  - dev_package/src/delivery_service/models.py
autonomous: true

must_haves:
  truths:
    - "System supports accessibility accommodations including extra time and alternative formats"
    - "System respects PNP (personal needs and preferences) from LTI launch"
  artifacts:
    - path: "dev_package/src/delivery_service/accommodations.py"
      provides: "Accommodation service with time multiplier and format support"
      min_lines: 40
    - path: "dev_package/src/lti_service/lti_service.py"
      provides: "PNP extraction from LTI launch claims"
      min_lines: 30
  key_links:
    - from: "dev_package/src/delivery_service/session_manager.py"
      to: "dev_package/src/delivery_service/accommodations.py"
      via: "time_limit adjusted by accommodation"
      pattern: "get_adjusted_time_limit"
    - from: "dev_package/src/lti_service/lti_service.py"
      to: "dev_package/src/delivery_service/models.py"
      via: "AccommodationProfile attached to session"
      pattern: "accommodation_profile"
---

<objective>
Add accessibility accommodations and PNP support — extra time, alternative formats, LTI launch integration.

Purpose: Meet LTI 1.3 accessibility requirements and support ADA compliance
Output: Accommodation service, PNP extraction, session integration
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-delivery-orchestration/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create accommodation service</name>
  <files>dev_package/src/delivery_service/accommodations.py</files>
  <action>
Create AccommodationService class that:
- Defines AccommodationType enum: EXTRA_TIME, LARGE_FONT, COLOR_CONTRAST, SCREEN_READER, BRAILLE
- Defines AccommodationProfile with accommodations: Dict[AccommodationType, Any]
- Provides get_adjusted_time_limit(base_minutes, profile) -> int (applies EXTRA_TIME multiplier, default 1.5x)
- Provides get_format_for_item(item_id, profile) -> str (returns format based on ACCOMMODATION)
- Validates accommodation conflicts (e.g., LARGE_FONT + SCREEN_READER)

Store multipliers in class constants. Support per-session accommodation overrides.
  </action>
  <verify>python -c "from dev_package.src.delivery_service.accommodations import AccommodationService, AccommodationType; print('OK')"</verify>
  <done>AccommodationService adjusts time and format based on profile</done>
</task>

<task type="auto">
  <name>Task 2: Add PNP extraction to LTI service</name>
  <files>dev_package/src/lti_service/lti_service.py</files>
  <action>
Extend LTI launch handling to extract PNP (Personal Needs and Preferences) from LTI claims:
- Parse launch presentation claims for locale, target link URI parameters
- Extract custom parameters prefixed with "launch_" that map to accommodations
- Build AccommodationProfile from extracted data
- Attach profile to AssessmentSession

Support these LTI params:
- `launch_extra_time` → EXTRA_TIME (multiplier value)
- `launch_font_size` → LARGE_FONT
- `launch_color_contrast` → COLOR_CONTRAST
- `launch_screen_reader` → SCREEN_READER (boolean)
  </action>
  <verify>python -c "from dev_package.src.lti_service.lti_service import LTIService; print('OK')"</verify>
  <done>LTI launch extracts PNP parameters into accommodation profile</done>
</task>

<task type="auto">
  <name>Task 3: Integrate accommodations into session timing</name>
  <files>dev_package/src/delivery_service/session_manager.py</files>
  <action>
Update SessionManager to:
- Accept accommodation_profile parameter when creating session
- Use AccommodationService.get_adjusted_time_limit() when computing session time
- Store accommodation_profile on AssessmentSession model
- Apply accommodations to item delivery (format selection)

Update AssessmentSession in models.py to include accommodation_profile field.
  </action>
  <verify>python -c "from dev_package.src.delivery_service.session_manager import SessionManager; print('OK')"</verify>
  <done>Session respects accommodation profile for timing and format</done>
</task>

</tasks>

<verification>
- [ ] AccommodationService applies correct time multiplier
- [ ] PNP parameters correctly extracted from LTI launch
- [ ] Session timing reflects accommodation adjustments
- [ ] No regression in existing delivery functionality
</verification>

<success_criteria>
System supports accessibility accommodations including extra time and respects PNP from LTI launch
</success_criteria>

<output>
After completion, create `.planning/phases/04-integrity-identity/{phase}-02-SUMMARY.md`
</output>