---
phase: 04-integrity-identity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dev_package/src/integrity_service/__init__.py
  - dev_package/src/integrity_service/risk_scorer.py
  - dev_package/src/integrity_service/behavioral_signals.py
  - dev_package/src/delivery_service/session_manager.py
  - dev_package/src/delivery_service/models.py
autonomous: true

must_haves:
  truths:
    - "System captures behavioral signals during assessment for integrity analysis"
    - "System provides risk scoring for manual integrity review"
  artifacts:
    - path: "dev_package/src/integrity_service/behavioral_signals.py"
      provides: "Behavioral signal aggregation from integrity events"
      min_lines: 40
    - path: "dev_package/src/integrity_service/risk_scorer.py"
      provides: "Risk scoring service with configurable thresholds"
      min_lines: 40
  key_links:
    - from: "dev_package/src/integrity_service/risk_scorer.py"
      to: "dev_package/src/delivery_service/session_manager.py"
      via: "session events query"
      pattern: "get_session_events"
    - from: "dev_package/src/integrity_service/behavioral_signals.py"
      to: "dev_package/src/delivery_service/models.py"
      via: "IntegrityEvent model"
      pattern: "IntegrityEvent"
---

<objective>
Build risk-tiered integrity verification — aggregate behavioral signals and produce risk scores for manual review.

Purpose: Extend Phase 2's integrity event logging into actionable risk assessment
Output: Risk scoring service, behavioral signal aggregator
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-delivery-orchestration/02-03-SUMMARY.md
@.planning/phases/03-scoring-reporting/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create behavioral signal aggregator</name>
  <files>dev_package/src/integrity_service/behavioral_signals.py</files>
  <action>
Create BehavioralSignalAggregator class that:
- Queries integrity events from audit ledger by session_id
- Aggregates into signal categories: TAB_SWITCH_COUNT, COPY_PASTE_COUNT, FULLSCREEN_VIOLATIONS, IDLE_TIME
- Computes derived signals: TOTAL_VIOLATIONS, VIOLATION_RATE (violations / session_duration_minutes)
- Returns SignalSummary with all computed values

Use existing audit ledger query patterns from Phase 2/3. Import from dev_package.src.audit_ledger.
  </action>
  <verify>python -c "from dev_package.src.integrity_service.behavioral_signals import BehavioralSignalAggregator; print('OK')"</verify>
  <done>BehavioralSignalAggregator can aggregate integrity events into signal categories</done>
</task>

<task type="auto">
  <name>Task 2: Create risk scoring service</name>
  <files>dev_package/src/integrity_service/risk_scorer.py</files>
  <action>
Create RiskScorer class that:
- Takes SignalSummary from BehavioralSignalAggregator
- Applies configurable weights for each signal type (default: TAB_SWITCH=10, COPY_PASTE=20, FULLSCREEN=30, IDLE=5)
- Computes weighted RISK_SCORE (0-100 scale)
- Categorizes risk level: LOW (0-30), MEDIUM (31-60), HIGH (61-100)
- Returns RiskAssessment with score, level, contributing_signals, and recommendations

Include RiskLevel enum and RiskAssessment dataclass. Support threshold configuration.
  </action>
  <verify>python -c "from dev_package.src.integrity_service.risk_scorer import RiskScorer, RiskLevel; print('OK')"</verify>
  <done>RiskScorer produces risk assessments with configurable thresholds</done>
</task>

<task type="auto">
  <name>Task 3: Wire risk scoring into session delivery</name>
  <files>dev_package/src/integrity_service/__init__.py</files>
  <action>
Update __init__.py to export BehavioralSignalAggregator and RiskScorer.
Add get_risk_assessment(session_id) function that combines both services.
This enables delivery API to surface risk scores after session completion.
  </action>
  <verify>python -c "from dev_package.src.integrity_service import get_risk_assessment; print('OK')"</verify>
  <done>Risk assessment accessible via get_risk_assessment() function</done>
</task>

</tasks>

<verification>
- [ ] BehavioralSignalAggregator successfully aggregates test events
- [ ] RiskScorer computes correct risk levels with default weights
- [ ] Risk assessment includes contributing signals and recommendations
- [ ] No circular dependencies with scoring/reporting modules
</verification>

<success_criteria>
System provides risk scoring for manual integrity review using aggregated behavioral signals
</success_criteria>

<output>
After completion, create `.planning/phases/04-integrity-identity/{phase}-01-SUMMARY.md`
</output>
