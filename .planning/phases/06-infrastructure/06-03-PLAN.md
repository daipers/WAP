---
phase: 06-infrastructure
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified: [src/delivery_service/websocket_handler.py, src/utils/redis_client.py, tests/verify_06.py]
autonomous: true

must_haves:
  truths:
    - "WebSocket state is shared across multiple application instances via Redis"
    - "Clients can reconnect to any pod and continue receiving timer updates"
    - "Infrastructure supports sticky sessions to minimize cross-pod state sync overhead"
  artifacts:
    - path: "src/utils/redis_client.py"
      provides: "Redis Sentinel client factory"
    - path: "src/delivery_service/websocket_handler.py"
      provides: "Distributed WebSocket coordination"
  key_links:
    - from: "src/delivery_service/websocket_handler.py"
      to: "src/utils/redis_client.py"
      via: "Pub/Sub for timer sync"
---

<objective>
Implement distributed WebSocket coordination using Redis Sentinel and finalize the high-availability infrastructure configuration to ensure stable, stateful delivery across scaled pods.

Purpose: Guarantee session continuity and timer synchronization in a horizontal scaling environment.
Output: Distributed WebSocket handler and infrastructure verification suite.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/STACK.md
@src/delivery_service/websocket_handler.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Redis Sentinel Client & Distributed Coordination</name>
  <files>src/utils/redis_client.py, src/delivery_service/websocket_handler.py</files>
  <action>
    Create `src/utils/redis_client.py`:
    - Implement an async Redis client factory that supports Sentinel for high availability.
    - Provide utility methods for session state caching and Pub/Sub.

    Refactor `src/delivery_service/websocket_handler.py`:
    - Replace in-memory `_active_connections` with a distributed tracking mechanism using Redis sets.
    - Implement a Pub/Sub listener for each session: when a timer update is published to `session:{id}:timer`, all pods with active connections for that session broadcast to their local clients.
    - Use a distributed lock (Redis) to ensure only one pod is "authoritative" for running the timer decrement loop for a given session.
  </action>
  <verify>Run two instances of the app and verify that a message sent to one is received by a client connected to the other via Redis Pub/Sub.</verify>
  <done>WebSocket handler is distributed and supports horizontal scaling.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize Infrastructure Verification Suite</name>
  <files>tests/verify_06.py</files>
  <action>
    Create a comprehensive verification script that:
    1. Checks if `pgcrypto` is active and encrypting/decrypting `IdentityService` data correctly.
    2. Simulates multiple application pods (mocked if K8s not available locally) to verify Redis-backed WebSocket synchronization.
    3. Verifies that `DB_ENCRYPTION_KEY` is required and handled securely.
    4. Validates that `pgAudit` (if enabled in DB config) is recording sessions correctly.
  </action>
  <verify>Run `python tests/verify_06.py` and ensure all checks pass.</verify>
  <done>Infrastructure and security foundation is verified and ready for institutional deployment.</done>
</task>

</tasks>

<verification>
Verify `src/utils/redis_client.py` uses Sentinel.
Confirm `src/delivery_service/websocket_handler.py` uses Pub/Sub for broadcasts.
Ensure `tests/verify_06.py` covers both PII encryption and scaling logic.
</verification>

<success_criteria>
1. WebSocket messages are successfully synchronized across multiple service instances.
2. The system can recover from a single pod failure without losing session state or timer progress.
3. Verification suite passes all infrastructure and security audits.
</success_criteria>

<output>
After completion, create `.planning/phases/06-infrastructure/06-03-SUMMARY.md`
</output>
