---
phase: 06-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: [dev_package/requirements.txt, src/utils/db.py, src/identity_service/identity.py, src/identity_service/models.py]
autonomous: true

must_haves:
  truths:
    - "PII fields (email) are stored encrypted in Postgres"
    - "Application handles encryption/decryption transparently via the DB layer"
    - "IdentityService uses PostgreSQL instead of in-memory storage"
  artifacts:
    - path: "src/utils/db.py"
      provides: "Postgres connectivity & pgcrypto integration"
    - path: "src/identity_service/models.py"
      provides: "SQLAlchemy models for candidates"
  key_links:
    - from: "src/identity_service/identity.py"
      to: "src/utils/db.py"
      via: "Database dependency injection"
---

<objective>
Implement application-layer PII encryption using `pgcrypto` and migrate the Identity Service from in-memory storage to a production-hardened PostgreSQL implementation.

Purpose: Secure sensitive candidate data and ensure persistence.
Output: Database utility layer and migrated Identity Service.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@src/identity_service/identity.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Production Dependencies & Implement DB Utility</name>
  <files>dev_package/requirements.txt, src/utils/db.py</files>
  <action>
    Update dev_package/requirements.txt with:
    - `sqlalchemy[asyncio]>=2.0`
    - `asyncpg>=0.29`
    - `cryptography>=42.0`
    - `redis[hiredis]>=5.0`

    Create `src/utils/db.py`:
    - Setup SQLAlchemy async engine and sessionmaker.
    - Implement a `PGcryptoString` type decorator or hybrid property that uses `pgp_sym_encrypt` and `pgp_sym_decrypt` via SQL expressions for transparent PII handling.
    - Use `os.getenv("DB_ENCRYPTION_KEY")` as the symmetric key.
  </action>
  <verify>Import `src.utils.db` and verify session factory initialization.</verify>
  <done>Database utility layer is ready with pgcrypto support.</done>
</task>

<task type="auto">
  <name>Task 2: Define Identity Models and Migrate IdentityService</name>
  <files>src/identity_service/models.py, src/identity_service/identity.py</files>
  <action>
    Create `src/identity_service/models.py`:
    - Define `Candidate` model with `candidate_id` (PK, UUID), `email` (using `PGcryptoString`), `consent_version`, `attempt_count`, etc.
    - Define `Session` model to store session state and metadata.

    Refactor `src/identity_service/identity.py`:
    - Replace in-memory `self.candidates` and `self.sessions` with database queries.
    - Use the async session from `src/utils/db.py`.
    - Ensure all CRUD operations (create_candidate, get_candidate, start_session) are now `async`.
  </action>
  <verify>Verify that `create_candidate` saves a record to Postgres with an encrypted email field.</verify>
  <done>Identity service is migrated to Postgres with PII encryption.</done>
</task>

</tasks>

<verification>
Check `src/utils/db.py` for pgcrypto implementation.
Verify `src/identity_service/identity.py` methods are async and use DB sessions.
Confirm `dev_package/requirements.txt` contains new dependencies.
</verification>

<success_criteria>
1. Identity Service successfully persists data to PostgreSQL.
2. Email addresses are stored as ciphertext in the database.
3. Service methods are non-blocking (async).
</success_criteria>

<output>
After completion, create `.planning/phases/06-infrastructure/06-02-SUMMARY.md`
</output>
