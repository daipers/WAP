---
phase: 06-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [Dockerfile, .dockerignore, k8s/app.yaml, k8s/db.yaml, k8s/redis.yaml, k8s/ingress.yaml]
autonomous: true

must_haves:
  truths:
    - "Application can be built as a Docker image"
    - "K8s manifests exist for HA deployment"
    - "Database and Redis are defined as StatefulSets"
  artifacts:
    - path: "Dockerfile"
      provides: "Containerization"
    - path: "k8s/app.yaml"
      provides: "FastAPI deployment"
    - path: "k8s/db.yaml"
      provides: "Postgres StatefulSet"
  key_links:
    - from: "Dockerfile"
      to: "dev_package/src/app.py"
      via: "CMD"
---

<objective>
Containerize the WAA-ADS FastAPI application and define the Kubernetes infrastructure required for high-availability deployment, including Postgres and Redis Sentinel.

Purpose: Enable scalable, reliable deployment in institutional environments.
Output: Dockerfile and K8s manifests.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Dockerfile and .dockerignore</name>
  <files>Dockerfile, .dockerignore</files>
  <action>
    Create a multi-stage Dockerfile for the FastAPI application.
    - Base: python:3.11-slim
    - Install production dependencies from dev_package/requirements.txt.
    - Copy dev_package/src and dev_package/templates.
    - Set PYTHONPATH to include /app/src.
    - Run using uvicorn: `uvicorn app:app --host 0.0.0.0 --port 8000 --workers 4`.
    - Create .dockerignore to exclude .git, .venv, __pycache__, and .planning.
  </action>
  <verify>Run `docker build -t waa-ads:v2.0 .` and ensure it completes successfully.</verify>
  <done>Docker image is buildable and contains the necessary source files.</done>
</task>

<task type="auto">
  <name>Task 2: Define HA Kubernetes Manifests (App, DB, Redis)</name>
  <files>k8s/app.yaml, k8s/db.yaml, k8s/redis.yaml</files>
  <action>
    Create K8s manifests for the following:
    - **k8s/app.yaml**: Deployment with 3 replicas, Service (ClusterIP), and HPA configuration.
    - **k8s/db.yaml**: Postgres StatefulSet with 3 replicas (1 primary, 2 read replicas) using a shared volume. Include a headless service for peer discovery.
    - **k8s/redis.yaml**: Redis Sentinel StatefulSet for high-availability state management.
    - Ensure all manifests use environment variables for configuration (DB_URL, REDIS_URL, etc.).
  </action>
  <verify>Run `kubectl apply --dry-run=client -f k8s/` to validate syntax.</verify>
  <done>Manifests are syntactically correct and follow HA patterns.</done>
</task>

<task type="auto">
  <name>Task 3: Configure Ingress with Sticky Sessions</name>
  <files>k8s/ingress.yaml</files>
  <action>
    Create an Ingress manifest (assuming Nginx Ingress Controller) with:
    - Path / routing to the app service.
    - Annotation: `nginx.ingress.kubernetes.io/affinity: "cookie"` for sticky sessions (required for WebSockets).
    - Annotation: `nginx.ingress.kubernetes.io/session-cookie-name: "route"`.
    - Basic TLS section (placeholder for cert-manager).
  </action>
  <verify>Verify ingress manifest contains the affinity annotations.</verify>
  <done>Ingress is configured to support stateful WebSocket connections across horizontal scaling.</done>
</task>

</tasks>

<verification>
Check all 4 files exist in k8s/ directory and Dockerfile is in root.
Verify that k8s/app.yaml defines 3 replicas.
Verify k8s/ingress.yaml contains affinity annotations.
</verification>

<success_criteria>
1. Dockerfile successfully builds a runnable image.
2. K8s manifests define an HA architecture for App, DB, and Redis.
3. Ingress is configured for sticky sessions.
</success_criteria>

<output>
After completion, create `.planning/phases/06-infrastructure/06-01-SUMMARY.md`
</output>
