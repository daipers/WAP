---
phase: 07-adaptive-engine
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified: [dev_package/src/delivery_service/delivery_api.py, dev_package/src/delivery_service/websocket_handler.py]
autonomous: true
must_haves:
  truths:
    - "Ability estimate (theta) and standard error (SE) are broadcast via WebSocket after each response."
    - "CAT engine meets precision requirement: SE < 0.3 within 20 items for simulated candidates."
  artifacts:
    - path: "dev_package/src/delivery_service/websocket_handler.py"
      provides: "Real-time broadcasting of theta/SE updates"
  key_links:
    - from: "dev_package/src/delivery_service/delivery_api.py"
      to: "dev_package/src/cat_service/irt_engine.py"
      via: "triggering estimate_theta_eap on answer submission"
    - from: "dev_package/src/delivery_service/delivery_api.py"
      to: "dev_package/src/delivery_service/websocket_handler.py"
      via: "triggering WebSocket broadcast after theta update"
---

<objective>
Enable real-time ability tracking and perform end-to-end verification of the CAT engine's precision and stability.

Purpose: Provide feedback to the user/system on estimation progress and ensure the engine meets psychometric standards.
Output: Real-time theta broadcasting and a comprehensive convergence verification report.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-adaptive-engine/07-01-SUMMARY.md
@.planning/phases/07-adaptive-engine/07-02-SUMMARY.md
@dev_package/src/delivery_service/delivery_api.py
@dev_package/src/delivery_service/websocket_handler.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Real-time Theta Updates on Answer Submission</name>
  <files>dev_package/src/delivery_service/delivery_api.py</files>
  <action>
    Update the `submit_answer` endpoint in `delivery_api.py`:
    - After recording a response, check if the session is in an ADAPTIVE section.
    - If it is, call `irt_engine.estimate_theta_eap` using all responses collected so far in the current session.
    - Update the session's `current_theta` and `theta_se`.
    - Trigger a WebSocket broadcast for the session to notify the client of the updated estimate.
  </action>
  <verify>
    Submit a correct answer and verify that the session's `current_theta` increases. Submit an incorrect answer and verify it decreases.
  </verify>
  <done>
    Ability estimates are updated in real-time after each response.
  </done>
</task>

<task type="auto">
  <name>Task 2: WebSocket Broadcast for Theta/SE</name>
  <files>dev_package/src/delivery_service/websocket_handler.py</files>
  <action>
    Update the `DeliveryWebSocketHandler` to include `current_theta` and `theta_se` in the periodic or event-driven updates sent to the client.
    Ensure the client receives these values in a structured format (e.g., `{"type": "theta_update", "theta": 0.5, "se": 0.28}`).
  </action>
  <verify>
    Connect a WebSocket client to a session, submit an answer, and verify that the client receives a message containing the updated theta and SE.
  </verify>
  <done>
    Theta/SE updates are broadcast in real-time via WebSockets.
  </done>
</task>

<task type="auto">
  <name>Task 3: CAT Convergence Verification (E2E Test)</name>
  <files>tests/verify_cat_convergence.py</files>
  <action>
    Create a verification script that:
    - Simulates 100 candidates with known "true" abilities (e.g., from -2 to 2).
    - Runs them through a 20-item adaptive assessment.
    - Records the final estimated theta and SE for each candidate.
    - Calculates the correlation between true and estimated theta.
    - Verifies that the average SE is < 0.3 after 20 items.
  </action>
  <verify>
    Run the script and output a summary of the convergence results.
  </verify>
  <done>
    CAT engine meets precision requirement (SE < 0.3 within 20 items).
  </done>
</task>

</tasks>

<verification>
- Run the convergence verification script and confirm SE < 0.3.
- Verify WebSocket updates are received by a test client.
</verification>

<success_criteria>
- Ability estimate converges on true ability in simulations.
- Real-time updates are visible to the client.
</success_criteria>

<output>
After completion, create `.planning/phases/07-adaptive-engine/07-03-SUMMARY.md`
</output>
