---
phase: 07-adaptive-engine
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified: [dev_package/src/delivery_service/session_manager.py, dev_package/src/delivery_service/delivery_api.py]
autonomous: true
must_haves:
  truths:
    - "The next item delivered in an ADAPTIVE section is selected in real-time based on current_theta."
    - "AssessmentSession tracks theta and standard error (SE) throughout the session."
  artifacts:
    - path: "dev_package/src/delivery_service/session_manager.py"
      provides: "Updated AssessmentSession with theta tracking and adaptive item selection"
  key_links:
    - from: "dev_package/src/delivery_service/session_manager.py"
      to: "dev_package/src/cat_service/irt_engine.py"
      via: "calling select_next_item_mfi during navigation"
---

<objective>
Integrate the IRT engine into the live assessment delivery flow by updating the session manager and navigation logic.

Purpose: Enable real-time, one-by-one item selection based on candidate performance.
Output: Updated session manager that supports dynamic item injection during navigation.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-adaptive-engine/07-01-SUMMARY.md
@dev_package/src/delivery_service/session_manager.py
@dev_package/src/delivery_service/delivery_api.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update AssessmentSession for Adaptive Tracking</name>
  <files>dev_package/src/delivery_service/session_manager.py</files>
  <action>
    Modify AssessmentSession to include:
    - current_theta: float = 0.0
    - theta_se: float = 1.0 (Standard Error)
    - quadrature_points: Optional[np.ndarray] (Cached for EAP)
    - quadrature_weights: Optional[np.ndarray]
    
    Update the `to_dict` and `from_dict` methods to persist these values.
    Add a method `add_item_to_session(item_data)` to dynamically append an item to the current section's items list.
  </action>
  <verify>
    Verify that a session can be created with theta values and that they persist through serialization/deserialization.
  </verify>
  <done>
    AssessmentSession tracks theta/SE and supports dynamic item addition.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate CATEngine into Session Navigation</name>
  <files>dev_package/src/delivery_service/session_manager.py, dev_package/src/delivery_service/delivery_api.py</files>
  <action>
    Update SessionManager.navigate:
    - When direction is "next" and the current section's selection_mode is ADAPTIVE:
      - Check if the current item is the last one *currently* in the list.
      - If it is, and we haven't reached the `items_to_select` limit for the section:
        - Call `irt_engine.select_next_item_mfi` using the session's `current_theta` and the section's `item_pool_ids`.
        - Retrieve the selected item from the content bank.
        - Append it to the session's current section items.
    
    Ensure `delivery_api.py` correctly handles the updated navigation flow and returns the new item.
  </action>
  <verify>
    Mock the IRT engine to return a specific item and verify that calling `/delivery/session/{id}/navigate?direction=next` appends that item and moves to it.
  </verify>
  <done>
    Navigation dynamically selects and injects items for adaptive sections.
  </done>
</task>

</tasks>

<verification>
- Perform a manual "dry run" using the API and verify that an adaptive section grows as the user navigates "next".
- Verify that non-adaptive (RANDOM/FIXED) sections still function as expected.
</verification>

<success_criteria>
- Adaptive sections deliver items one-by-one rather than having all items pre-assigned at session start.
- navigation_mode=LINEAR is respected during adaptive delivery.
</success_criteria>

<output>
After completion, create `.planning/phases/07-adaptive-engine/07-02-SUMMARY.md`
</output>
