---
phase: 05-audit-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dev_package/src/analytics_service/__init__.py
  - dev_package/src/analytics_service/metrics.py
  - dev_package/src/analytics_service/dif_detector.py
  - dev_package/src/analytics_service/dashboard.py
  - dev_package/src/app.py
  - dev_package/templates/analytics_dashboard.html
autonomous: true

must_haves:
  truths:
    - "System provides item performance analytics dashboard"
    - "System calculates psychometric metrics (difficulty, discrimination, reliability)"
    - "System detects item bias using DIF analysis"
  artifacts:
    - path: "dev_package/src/analytics_service/metrics.py"
      provides: "Psychometric calculations (difficulty, discrimination, Cronbach's alpha)"
      min_lines: 50
    - path: "dev_package/src/analytics_service/dif_detector.py"
      provides: "DIF detection using Mantel-Haenszel chi-square method"
      min_lines: 50
    - path: "dev_package/src/analytics_service/dashboard.py"
      provides: "Dashboard data aggregation API"
      min_lines: 30
  key_links:
    - from: "dev_package/src/analytics_service/dashboard.py"
      to: "dev_package/src/analytics_service/metrics.py"
      via: "calculate_item_difficulty, calculate_discrimination_index"
      pattern: "import"
    - from: "dev_package/src/analytics_service/dashboard.py"
      to: "dev_package/src/analytics_service/dif_detector.py"
      via: "detect_dif_chi_square"
      pattern: "import"
---

<objective>
Build item performance analytics dashboard with psychometric metrics and DIF detection for fairness analysis.

Purpose: Provide analytics dashboard showing item performance metrics and fairness analysis
Output: Analytics service with dashboard endpoints
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create psychometric metrics module</name>
  <files>dev_package/src/analytics_service/metrics.py</files>
  <action>
Create psychometric metrics functions:
- calculate_item_difficulty(responses): float - p-value as proportion correct
- calculate_discrimination_index(responses): float - point-biserial correlation
- calculate_cronbach_alpha(item_matrix): float - internal consistency
- calculate_confidence_interval(responses): tuple - Wilson score interval

Use pandas for data manipulation and scipy.stats for calculations.
  </action>
  <verify>python -c "from dev_package.src.analytics_service.metrics import calculate_item_difficulty, calculate_discrimination_index, calculate_cronbach_alpha; print('OK')"</verify>
  <done>Psychometric metrics module exports difficulty, discrimination, and reliability functions</done>
</task>

<task type="auto">
  <name>Task 2: Create DIF detector module</name>
  <files>dev_package/src/analytics_service/dif_detector.py</files>
  <action>
Create DIF detection functions:
- detect_dif_chi_square(item_responses, group_membership, ability_estimate): dict
  - Uses Mantel-Haenszel chi-square test
  - Returns chi2, p-value, classification (no_DIF, minor_DIF, moderate_DIF, severe_DIF)
  - Includes reference/focal pass rates
- detect_dif_logistic(item_responses, group_membership, ability_estimate): dict
  - Uses logistic regression with ability matching
  - Returns odds ratio, effect size, classification

Support configurable group attributes (gender, ethnicity, disability_status).
  </action>
  <verify>python -c "from dev_package.src.analytics_service.dif_detector import detect_dif_chi_square, detect_dif_logistic; print('OK')"</verify>
  <done>DIF detector module exports chi-square and logistic regression methods</done>
</task>

<task type="auto">
  <name>Task 3: Create analytics dashboard API</name>
  <files>dev_package/src/analytics_service/dashboard.py</files>
  <action>
Create analytics dashboard endpoints:
- GET /api/analytics/item-performance?assessment_id=xxx
  - Returns list of items with difficulty, discrimination, response_count
- GET /api/analytics/fairness-report?assessment_id=xxx&group_attribute=gender
  - Returns items with DIF analysis results
- GET /api/analytics/export?assessment_id=xxx&format=csv
  - Exports item performance data to CSV

Use existing database patterns from Phase 1-3.
  </action>
  <verify>python -c "from dev_package.src.analytics_service.dashboard import router; print('OK')"</verify>
  <done>Analytics dashboard API provides item performance and fairness endpoints</done>
</task>

<task type="auto">
  <name>Task 4: Create analytics dashboard UI</name>
  <files>dev_package/templates/analytics_dashboard.html</files>
  <action>
Create analytics dashboard HTML page:
- Item performance table with difficulty, discrimination columns
- DIF results section showing biased items
- Chart.js bar chart for item difficulty distribution
- Export button for CSV download

Use Chart.js 4.x for visualization. Include basic styling.
  </action>
  <verify>ls dev_package/templates/analytics_dashboard.html</verify>
  <done>Analytics dashboard displays item metrics and DIF results visually</done>
</task>

</tasks>